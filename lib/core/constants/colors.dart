import 'package:flutter/cupertino.dart';

class AppColors {
  AppColors._();

  // Premium Dark Mode Fintech Palette
  static const Color background = Color(0xFF0A0C10); // Ultra deep slate black background
  static const Color surface = Color(0xFF161920);    // Crisp dark card surface
  static const Color border = Color(0xFF222630);     // Very thin elegant dark border
  
  static const Color primary = Color(0xFF0E76FD);    // High-energy premium cobalt/neon blue
  static const Color profit = Color(0xFF0ECB81);     // Neon emerald green (for gains/longs)
  static const Color loss = Color(0xFFF6465D);       // Neon crimson red (for drops/shorts)
  
  static const Color textPrimary = Color(0xFFF8FAFC);   // Bright white for high legibility
  static const Color textSecondary = Color(0xFF8F9CAE); // Slate gray for secondary details
  
  static const Color separator = Color(0xFF1D222F);
}
